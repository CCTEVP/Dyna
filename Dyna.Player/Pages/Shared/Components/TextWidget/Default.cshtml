@model TextWidgetClass
@using Dyna.Player.Utilities

<div id="text-widget-@(Model.Identifier ?? Guid.NewGuid().ToString("N"))" class="@(Model.Classes ?? "")" style="@ConversionUtilities.GenerateCssStyleString(Model.Styles)">
    @if (Model.Value != null)
    {
        @Model.Value
    }
</div>