@model TextWidgetClass
@using Dyna.Player.Utilities
@using Dyna.Shared.Classes.Components.Widgets


<div id="text-widget-@(Model.Id)" data-name="@(Model.Identifier ?? Guid.NewGuid().ToString("N"))" class="@(Model.Classes ?? "")" style="@ConversionUtilities.GenerateCssStyleString(Model.Styles)">
    @if (Model.Value != null)
    {
        @Model.Value
    }
</div>